import styled from "styled-components/native";

const ErrorText = styled.Text`
  color: red;
  align-self: flex-end;
  font-weight: 100;
  font-size: 14px;
  margin-top: 3px;
`

export default ErrorText